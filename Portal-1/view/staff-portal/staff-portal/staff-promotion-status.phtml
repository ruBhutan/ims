<?php
 // module/GoodsTransaction/view/goods-transaction/goods-transaction/add-goods-recieved-purchased.phtml:

 $title = 'Promotion Status';
 $this->headTitle($title);
?>

<div class="right_col" role="main">
    <div class=" ">
        <div class="col-md-12 col-sm-12 col-xs-12">
            <div class="x_panel">
        <div class="x_title">
        <h2><?php echo $this->escapeHtml($title); ?></h2>
        <ul class="nav navbar-right panel_toolbox">
          <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a></li>
        </ul>
        <div class="clearfix"></div>
        </div> 

            <!-- Start of table listing item to issue -->
                <div class="x_panel">
                  <h5><i>*The following is promotion list.</i></h5>
                  <div class="x_content">
                     <div class="table-responsive">
                      <table class="table table-striped jambo_table bulk_action">
                        <thead>
                          <tr class="headings">    
                            <th class="column-title">Sl.No. </th>
                            <th class="column-title">Promotion Type</th>
                          <th class="column-title">Security Clearance No</th>
                            <th class="column-title">Audit Clearance No</th>
                            <th class="column-title">Status</th>
                          </tr>
                        </thead>
                        <tbody>
                            <?php $i=1; ?>
                                <?php foreach ($this->promotionDetails as $status) : ?>
                                <tr>
                                  <th scope="row" width="8%"><?php echo $i; ?></th>
                                  <td><?php echo $this->escapeHtml($status['promotion_type']); ?></td>
                                  <td><?php echo $this->escapeHtml($status['security_clearance_no']); ?></td>
                                  <td><?php echo $this->escapeHtml($status['audit_clearance_no']); ?></td>
                                  <td><?php echo $this->escapeHtml($status['promotion_status']); ?></td>
                                </tr>
                                <?php $i++; ?>
                                <?php endforeach; ?>
                        </tbody>
                      </table>
                     </div>
                 </div>                 
             </div>
        </div> 
    </div>
</div>
</div>

